# Aero Throttle

Parametric CAD project.

Start-prompt templates for the two-agent workflow are in prompts/.

Configure the CAD engine in cad_config.json; build, export, render, and validate with python scripts/build.py.

Configure the independent validation suite using alidation_config.json.
